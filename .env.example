# Optional. Leave unset while using `npm run dev` to use the Vite proxy (same origin, no CORS).
# Set to your API origin if you call the backend directly (e.g. http://localhost:5000).
VITE_API_URL=http://localhost:5000
